# تغییرات فاز ۳الف — سخت‌سازی + فیچرهای محصولی مستقل

این فاز نسبت به فازهای قبل ریسک کمتری دارد — همه‌چیز افزوده (additive) است، چیزی جایگزین یا بازطراحی نشده.

## تصمیم مهم: فاز ۳ به دو بخش تقسیم شد

نقشه‌راه اصلی «چندکاربری/تیم» را هم جزو فاز ۳ گذاشته بود. موقع شروع کار متوجه شدم این عملاً یک بازطراحی امنیتی-حساس بزرگ است (نیاز به مهاجرت auth به دیتابیس + مدل نقش/workspace کامل) — دقیقاً مثل تصمیمی که در فاز ۱ برای auth گرفتم. به‌جای اینکه آن را با فیچرهای کوچک‌تر و کم‌ریسک‌تر قاطی کنم، **فقط فیچرهای مستقل و کم‌ریسک** را در این پاس انجام دادم و چندکاربری را به یک «فاز ۳ب» جدا موکول کردم.

## چه چیزی اضافه شد

### ۱. محدودیت نرخ درخواست (Rate Limiting)
کل `/api` حالا با `express-rate-limit` محافظت می‌شود (۶۰۰ درخواست در ۱۵ دقیقه به ازای هر IP) — دفاع تکمیلی، جدا از قفل brute-force اختصاصی که از قبل روی لاگین بود.

### ۲. متریک واقعی سرور
`GET /api/system-metrics` حالا CPU/RAM واقعی سرور را برمی‌گرداند (از ماژول `os` نود — بدون هیچ وابستگی خارجی). فرانت‌اند (`App.tsx`) به‌روز شد تا از این endpoint واقعی بخواند، نه شبیه‌سازی تصادفی قبلی.

**هنوز فیک مانده:** نمودار Uptime ۲۴ ساعته (`UptimeChart.tsx`) — چون نیاز به یک سیستم نمونه‌برداری دوره‌ای و ذخیره‌ی تاریخچه در Postgres دارد (یک زیرسیستم کوچک مستقل)، عمداً به فاز ۳ب موکول شد.

### ۳. تشخیص محتوای تکراری
تنظیم جدید «رد کردن محتوای تکراری» برای هر پل (در تب تمیزسازی پنل تنظیمات). اگر فعال باشد، پستی که متنش (بعد از پردازش) در ۷ روز اخیر برای همان پل قبلاً ارسال شده، دوباره ارسال نمی‌شود — مفید برای کانال‌های خبری که گاهی همان خبر را دوباره منتشر می‌کنند.

### ۴. یک نکته‌ی مستندسازی (بدون کد جدید)
شاید ندانید، ولی **«چند کانال مبدأ به یک مقصد»** یا **«یک مبدأ به چند مقصد»** از همین الان کار می‌کند — کافی است چند «پل» جدا با کانال‌های مشترک بسازید. معماری از قبل این را پشتیبانی می‌کرد.

## چه چیزهایی تغییر کرد

- `server.ts`: rate limiter + `GET /api/system-metrics`.
- `src/App.tsx`: کارت‌های CPU/RAM حالا از API واقعی می‌خوانند.
- `src/schemas.ts`: فیلد `skipDuplicateContent`.
- `prisma/schema.prisma` + یک migration جدید: مدل `SentContentHash`.
- `src/server/storage.ts`: متدهای `hasSeenContentHash`/`recordContentHash`.
- `src/server/telegram.ts`: ادغام چک تکراری در `processSinglePost`.
- `src/components/ConnectionRulesModal.tsx`: toggle جدید در تب تمیزسازی.
- تست‌های جدید در `tests/integration/incoming-push.test.ts` (بخش «Duplicate content detection»).
- اسکیل پروژه کامل به‌روز شد؛ نقشه‌راه به فاز ۳الف/۳ب تقسیم شد.

## قدم بعدی

فاز ۳ب (چندکاربری/workspace/نقش) یک تصمیم و کار جدا و بزرگ است — پیشنهاد می‌کنم وقتی به آن رسیدیم، یک گفتگوی مستقل و دقیق درباره‌ی مدل نقش/دسترسی داشته باشیم، نه فقط «ادامه بده». تا آن موقع، این نسخه از نظر فنی برای یک کاربر تنها (یا چند نفر که به هم اعتماد کامل دارند و یک حساب مشترک استفاده می‌کنند) کاملاً آماده است.
