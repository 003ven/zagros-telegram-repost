# تغییرات فاز ۲ — موتور Telethon به‌جای اسکرِیپر (push-based)

## ⚠️ این پرریسک‌ترین فاز تا اینجاست — لطفاً با احتیاط بیشتری تست کنید

بر خلاف فاز ۰ و ۱ که فقط زیرساخت/ذخیره‌سازی را عوض کردند، این فاز رفتار زنده‌ی ارتباط با Telegram API واقعی را عوض می‌کند — چیزی که **به هیچ وجه نمی‌شد بدون یک اکانت Telegram واقعی تست کرد**. کد را با دقت و بر پایه‌ی مستندات رسمی Telethon نوشتم، اما تا وقتی روی یک اکانت واقعی امتحان نشود، احتمال نیاز به اصلاح جزئی وجود دارد.

## این فاز کاملاً اختیاری و بی‌خطر است

**نکته‌ی مهم:** اگر هیچ کاری نکنید (سرویس یوزربات را روشن نکنید یا `NODE_WEBHOOK_URL` را تنظیم نکنید)، برنامه **دقیقاً مثل قبل** با همان اسکرِیپ دوره‌ای کار می‌کند. هیچ‌چیز برای کاربرانی که یوزربات را استفاده نمی‌کنند عوض نشده. این موتور جدید فقط وقتی فعال می‌شود که صراحتاً آن را روشن کنید.

## این فاز چه کاری انجام می‌دهد

به‌جای این‌که سرور هر ۱۵ ثانیه صفحه‌ی عمومی کانال را بررسی کند (با تأخیر تا ۱۵ ثانیه)، اگر سرویس یوزربات را با تنظیمات کامل اجرا کنید، اکانت یوزربات مستقیم عضو کانال‌های مبدأ می‌شود و همان لحظه که پستی منتشر می‌شود، به سرور اطلاع می‌دهد — یعنی تقریباً بدون تأخیر.

## چطور فعالش کنم

با Docker (ساده‌ترین راه):
1. `docker-compose.yml` از قبل سرویس `userbot` را با `NODE_WEBHOOK_URL` تنظیم‌شده دارد.
2. فقط باید `TELEGRAM_API_ID`، `TELEGRAM_API_HASH` (از my.telegram.org) و `USERBOT_SECRET` را در `.env` بگذارید.
3. یک‌بار (فقط یک‌بار، خارج از Docker) `python3 userbot/login.py` را اجرا کنید تا فایل session ساخته شود (نیاز به کد تأیید پیامکی/۲FA دارد).
4. `docker compose up --build` — از این به بعد پل‌های فعال خودکار وارد حالت push می‌شوند.

بدون Docker: همین مراحل، فقط `NODE_WEBHOOK_URL` را دستی هنگام اجرای `userbot_service.py` export کنید (به آدرس Node اشاره کند، مثلاً `http://127.0.0.1:3000`).

## ریسک‌های شناخته‌شده و آگاهانه (لطفاً قبل از اعتماد کامل بخوانید)

1. **آلبوم‌ها هنوز کامل پشتیبانی نمی‌شوند.** یک آلبوم چند‌عکسی در مسیر push به‌جای یک پیام گروهی، چند پیام جدا می‌شود. مسیر قدیمی (اسکرِیپ، وقتی یوزربات نیست) این را درست انجام می‌دهد.
2. **تبدیل متن به HTML** (`telethon.extensions.html.unparse`) با ورودی‌های واقعی و پیچیده‌ی تلگرام (لینک‌های تو در تو، فرمت‌های خاص) تست نشده.
3. **اگر سرویس یوزربات ری‌استارت شود**، لیست کانال‌های تحت نظر (در حافظه) پاک می‌شود. باید یا Node را هم ری‌استارت کنید (که خودش دوباره برای پل‌های فعال watch می‌فرستد) یا دستی هر پل را «راه‌اندازی مجدد» کنید.
4. **بدون retry خودکار** اگر لحظه‌ی رسیدن پیام، سرور Node موقتاً پایین باشد، آن پیام خاص گم می‌شود (چون یا در حالت push هستید یا poll، نه هر دو هم‌زمان).

## چه چیزهایی تغییر کرد

- `userbot/userbot_service.py`: هندلر `client.on(events.NewMessage())`، endpoint های `/watch`, `/unwatch`, `/media/{token}`.
- `src/server/telegram.ts`: تابع مشترک `processSinglePost` (استخراج‌شده از منطق قدیمی `pollConnection`)، `handleIncomingPushMessage` جدید، `startMonitoring`/`stopMonitoring` دوحالته.
- `server.ts`: route جدید `POST /api/internal/incoming-message` (با `X-Userbot-Secret`، نه لاگین کاربر).
- `src/schemas.ts`: `IncomingPushMessageSchema`.
- `docker-compose.yml`: `NODE_WEBHOOK_URL` برای سرویس userbot.
- `.env.example`, `deploy/DEPLOY_FA.md`: مستندسازی.
- تست‌های جدید: `tests/integration/incoming-push.test.ts`.
- **رفع دو گپ کوچک از فاز‌های قبل که موقع کار روی این فاز پیدا شدند:** `DATABASE_URL` در `ecosystem.config.cjs` و `render.yaml` فراموش شده بود (فاز ۱)؛ `render.yaml` هم `startCommand` را طوری آپدیت کردم که خودش قبل از استارت `prisma migrate deploy` را بزند.

## قدم بعدی

بعد از تست واقعی روی یک اکانت Telegram، می‌توانیم به فکر رفع محدودیت آلبوم و افزودن retry/resync خودکار باشیم — یا برویم سراغ فاز ۳ (چندکاربری و فیچرهای محصولی).
